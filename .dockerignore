# Keep the build context small and force a clean install/build in-image.

# Node
web/node_modules
web/.next
web/out
web/.env.local
npm-debug.log*

# Go build artifacts
server/server
**/*.test

# VCS / editor / OS
.git
.gitignore
**/.DS_Store

# Docs / plans not needed in the image
*.md
